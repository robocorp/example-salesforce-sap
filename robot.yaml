tasks:
  List accounts from SAP:
    robotTaskName: List accounts from SAP
  List accounts from Salesforce:
    robotTaskName: List accounts from Salesforce
  List accounts from Salesforce (no browser):
    robotTaskName: List accounts from Salesforce (no browser)
  Create New Accounts to Salesforce:
    robotTaskName: Create New Accounts to Salesforce
  Account Compare Salesforce Against SAP:
    robotTaskName: Compare Salesforce and SAP accounts
  Fix Comparison Errors:
    robotTaskName: Fix comparison errors
  Fix Account Name in SFDC:
    robotTaskName: Fix Account Name in SFDC


condaConfigFile: conda.yaml
artifactsDir: output
PATH:
  - .
PYTHONPATH:
  - .
  - libraries
  - resources
ignoreFiles:
  - .gitignore
